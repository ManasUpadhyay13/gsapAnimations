/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', "raw.githubusercontent.com", "picsum.photos"],
    },
};

export default nextConfig;
